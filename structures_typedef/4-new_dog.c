#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "dog.h"
/**
 * new_dog - ooo
 * @name: ooo
 * @age: ooo
 * @owner: ooo
 * Return: ooo
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	struct dog_t d;

	d = malloc

	d = {
		name = name,
		age = age,
		owner = owner,
	}

	if (d == NULL)
        {
                return (NULL);
        }
	return (d);
}

